<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Flight Booking Dashboard" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="application-name" content="Flight Booking">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  </head>

  <body class="bg-gray-100 h-screen flex overflow-hidden">
    <!-- Sidebar -->
    <aside class="w-64 bg-white shadow-md flex-shrink-0 flex flex-col">
      <div class="p-6 text-xl font-bold border-b">Flight Dashboard</div>
      <nav class="flex-1 px-4 py-6">
        <ul class="space-y-4">
          <!--<li>
            <%= link_to "Overview", class: "block py-2 px-4 rounded hover:bg-gray-100" %>
          </li> -->
          <li>
            <%= link_to "Flights", admin_flights_path, class: "block py-2 px-4 rounded hover:bg-gray-100" %>
          </li>
          <li>
            <%= link_to "Bookings", admin_bookings_path, class: "block py-2 px-4 rounded hover:bg-gray-100" %>
          </li>
          <li>
            <%= link_to "Passengers", admin_passengers_path, class: "block py-2 px-4 rounded hover:bg-gray-100" %>
          </li>
        
        </ul>
      </nav>
    </aside>

    <!-- Main content -->
    <div class="flex-1 flex flex-col overflow-auto">
      <!-- Top navigation / header -->
      <!-- <header class="bg-white shadow p-4 flex justify-between items-center">
           <h1 class="text-2xl font-bold"><%= content_for?(:page_title) ? yield(:page_title) : "Dashboard" %></h1> 
            
        
      </header>-->

      <!-- Page content -->
      <main class="p-6">
        <%= yield %>
      </main>
    </div>
  </body>
</html>
